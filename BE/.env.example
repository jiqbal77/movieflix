# Environment Configuration
#
# This project uses environment-specific configuration files:
# - .env (base file, only contains NODE_ENV)
# - .env.development (development-specific variables)
# - .env.staging (staging-specific variables)
# - .env.production (production-specific variables)
#
# The app loads .env first, then loads the environment-specific file
# based on NODE_ENV value. Variables in environment-specific files
# override variables in .env.
#
# To use:
# 1. Copy this file to .env and set NODE_ENV
# 2. Create environment-specific files (.env.development, .env.staging, .env.production)
# 3. Add environment-specific values to each file

# Server Configuration
PORT=3000
NODE_ENV=development

# OMDb API Configuration
# Get your API key from: http://www.omdbapi.com/apikey.aspx
OMDB_API_KEY=your_omdb_api_key_here

# CORS Configuration
# Comma-separated list of allowed origins
ALLOWED_ORIGINS=http://localhost:3000

# Swagger Documentation
# Basic auth credentials for accessing API documentation
SWAGGER_USER=admin
SWAGGER_PASSWORD=admin
